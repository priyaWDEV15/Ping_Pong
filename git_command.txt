1. local project to github

cd project_dir
git init
git remote add origin repo_url.git
git branch -M main
git add .
<not a command create a .gitignore file>
git commit -m "initial commit"
git push -u origin main

normally

git add .
git commit -m "message"
git pull
git push


2. first create repo then start